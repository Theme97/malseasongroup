To run, execute `coffee counter.coffee <topicID>`

Progress info goes to STDOUT and match failures go to STDERR.

Output is saved to `tally-<topicID>.txt`

NOTE: This currently only works for OP/ED threads.

